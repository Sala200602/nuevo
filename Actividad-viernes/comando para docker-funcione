#Entrar a la carpeta donde esta el archivo docker-compose.yml

## 1. Ingresamos en la terminal desde donde tenemos nuestro archivo
cd C:\Users\Usuario\OneDrive\Documentos\4to Semestre\base de datos distribuida\Actividad-viernes

#2. Verificamos que tengamos docker
docker version 

## 3. Verificamos que tengamos el Docker Compose Activo
docker compose version 

## 4. Apaga y elimina contenedores
docker compose down

## 5. Apaga y borra todo los datos, empezando desde 0
docker compose down -v
